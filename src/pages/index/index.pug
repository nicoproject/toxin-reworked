extends ../default-layout/default-layout

block variables
  - var title = 'Toxin ReWorked';
  - var description = 'Frontend test project';
  - var pageClass = 'index-page';

block content
  .container


block script
  script(type='text/javascript').
    const foo = true;
    let bar = function() {};
    if (foo) {
    bar(1 + 5)
    }
